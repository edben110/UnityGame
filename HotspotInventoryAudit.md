Hotspot & Inventory Audit Report

Generated: May 16, 2026

Summary:
- Scanned code for all places that grant inventory items via MapHotspot (`grantItemId`) and Dialogue (`addInventoryItemId`).
- Implemented fixes:
  - DialogueRunner now attempts to register runtime item sprites from Resources when a dialogue line grants an item.
  - InventoryOverlayCanvas uses a procedural placeholder sprite when an item has no catalog icon, avoiding invisible slots.
  - DoorTrigger chapter-3 validation relaxed to require Simon room key + at least one NPC talked; Ben+book is now optional.

Discovered inventory IDs (from code builders & hotspots):
- libro_contabilidad
- foto_tablero_corcho
- foto_padre_hijo
- HS_Unfinished_Letter (also appears as HS_Unfinished_Letter in builders)
- unfinished_letter (legacy)
- llave_pequena
- GalleryKey
- BasementKey
- mapa_ala_norte
- papeles_lisa
- caja_puzzle_1
- codigo_4729
- carpeta_evidencia
- diario_final
- relicario_plata
- mapa_actualizado (naming variants may exist)

Checklist for each hotspot/item (manual inspector verification required):
- [ ] MapHotspot.grantItemId matches an entry in InventoryCatalog (case-insensitive). If not, add to InventoryCatalog or use consistent id.
- [ ] InventoryCatalog entry has `icon` assigned. If missing, add sprite asset and assign in the catalog.
- [ ] For items added via Dialogue (`addInventoryItemId`), confirm there is either a catalog entry with icon or a Resources sprite at one of recommended paths:
      - `Resources/Sprites/{itemId}`
      - `Resources/Sprites/{itemId.ToLower()}`
      - `Resources/Sprites/Inventory/{itemId}`
      - `Resources/Icons/{itemId}`
      - `Resources/Items/{itemId}`
  If present, the DialogueRunner will register a runtime item at add-time.
- [ ] MapHotspot fields: `setFlagOnInteract`, `consumeAfterUse`, `hideAfterPickup` are set correctly per hotspot design.

Next actionable items (recommended):
1. Open Unity Editor and run the following play checks for each critical item:
   - Pickup via hotspot OR trigger the dialogue line that grants the item.
   - Open inventory (`I`) and verify the sprite appears, name shows, and selection works.
2. For any items that still show placeholder:
   - Add the corresponding sprite to `Resources/Sprites/` using one of the naming patterns above, or add the icon to `InventoryCatalog` entry.
3. For hotspots autogenerated by `GenerateHotspots`, verify the generated hotspots include `itemId` values where intended.
4. If there are scene-specific hotspots missing (collider-only), ensure `MapHotspot.grantItemSprite` or `grantItemId` are set in the Inspector.

Notes:
- The code changes ensure dialogue-granted items are registered in the catalog (if a matching resource exists) and that the UI will never render an entirely invisible slot (it will use a procedural placeholder). However, for correct visuals and narrative polish, assigning proper icons in `InventoryCatalog` is recommended.
- A full inspector audit of scene objects (Hotspots parent and individual MapHotspot components) is still required to fully guarantee no missing sprites or flags at runtime.

If you want, I can:
- Produce a list of GameObjects in scenes that contain `MapHotspot` components by scanning scene files (best-effort text search) to help the inspector pass, or
- Automatically attempt to map item ids to Resources sprites and register them into `InventoryCatalog` on startup (requires deciding naming conventions).
